
%article.video_category
  %header
    %h3= link_to "Disney Docu-Movies"
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2{title: "#{video.title}: #{video.description}"}
        = link_to video_path(video) do
          %img(src="#{video.small_cover_url}")